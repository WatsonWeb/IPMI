# Task 36 — provisional Operations handoff

September 24, 2026 · [Task](../../36-kbyg-operations-content.md) · [54-row matrix](MATRIX.md) · [Exact matrix and field values](coverage-matrix.json)

## Delivered scope and authority

The 26 exact native events from task 35 each have Delegate and Sponsor planning rows. The past HCHR September 2026 pair remains, giving 54 rows across 27 Institutes. Four existing IDs/URLs are retained; exactly 50 missing pages are prepared for task 25. This is a usable provisional content handoff, not a claim of Operations approval, schedule completeness or 54 published pages.

The user's placeholder instruction supersedes the earlier task text forbidding placeholders. New copy explicitly says Placeholder; unknown staff, images, deadline blocks and links remain empty. No event facts, staff assignments, deadlines, URLs or approvals were invented. Native event title/year/date/location facts remain provisional task 35 data. Use stored calendar-date components without timezone shifting.

## Exact task 25 import and editorial contract

- [Creation JSON](task25-create-pack.json) contains all 51 actual field slugs for each of the 50 missing pages; drafts are intentional creation defaults, not final published state. [CSV](task25-create-pages.csv) contains only those 50 rows with actual display-name headers. Neither artifact has been imported.
- CSV `Institute` contains the native Institute **slug**, and `Audience` contains lowercase native option labels. Task 25 must inspect Webflow Import mappings and preview resolved references/options before confirming creation. JSON carries exact reference/option IDs for reconciliation; it is not authorization to use API writes. Do not map blanks to existing pages or duplicate the original four. CMS creates new page IDs; record every assigned ID and re-run pair uniqueness checks.
- `name`, `slug`, `institute`, `audience`, every heading/body/CTA label, optional field and ordered reference list are explicit in each row. `hero-eyebrow` derives solely from known event calendar dates. The template continues binding the referenced Institute title, year, venue, colors, map/address and gallery. Native imagery on the Institute is not a new page-level image assignment.
- New `operations-lead`, `hero-image`, `hub-image`, `hub-url` and `support-url` are null; all five block lists are empty. Preserve native hidden-empty images, contact notice and noninteractive empty links. Do not borrow Tori/Katrina, HCHR hotel rates or any deadline blocks. Blank optional image alts do not justify a fabricated photo.
- Exact new rich text is included as HTML in both artifacts. Unknown Welcome, Hub, Reservation and Sponsor Support copy is labeled. Empty preparation, key-date, agenda, experience and FAQ lists use existing native notices. Sponsor Support and Delegate FAQ are independent audience branches; Delegate support fields deliberately stay empty.
- Task 26 uses `agenda-title: Institute Agenda` for new pages, retains `#agenda` and supplies its compact link/pending state. Existing four agenda titles remain unchanged for task 26 to update. Do not reconstruct an agenda timetable from calendar start/end dates.
- Task 27 must handle `hub-url` and **`support-url`** independently. The two original Sponsor pages currently contain `https://example.com/sponsor-hub` in both fields; these inherited active placeholders are known task 27 corrections, never copied to new pages. Original Delegate `hub-url` values are `mailto:kbrightling@ipmievents.com` and `mailto:tdiclemente@ipmievents.com` with `OPEN ATTENDEE HUB` labels. Preserve useful contact, clarify its label and provide a separate pending actual MeetMax state; mailto is not a verified login URL.

[Schema](page-schema.json) identifies actual field IDs/types and required flags. Only Name/Slug are schema-required; this does not make absent final Operations content approved. [1,232 exact pending field entries](placeholder-fields.json) identify 1,225 prepared empty/placeholder fields plus seven applied copy placements by event, audience, page slug and field. The matrix's `instituteSourceFields` preserves exact available Institute KBYG values for review; final approval is pending even where native data exists.

## Native CMS corrections and rollback

Only the existing EHS pair was edited through Chrome CMS. No page/block was created or deleted, and no schema, Institute, Staff, template or runtime asset was edited.

[Exact before/after delta](existing-delta.json) records every changed field, original ordered references and publish state. [Before pages](pages-before.json), [after pages](pages-after-save.json), [before blocks](blocks-before.json), [after blocks](blocks-after.json), and [original consumers](ownership-before.json) allow exact rollback.

EHS Sponsor: `preparation`, `agenda-days` and `experience` cleared; `preparation-title`, `preparation-intro`, `agenda-intro`, `experience-intro` updated. EHS Delegate: those same fields plus `faqs` cleared and `welcome-body` changed to `<p>Placeholder - Operations copy pending.</p>`. This removes the wrong Healthcare HR discipline and stale three-step promise. Both preparation titles now read `Preparing for your Institute.`. The three intro fields use the exact labeled pending copy in the delta/register. Native CMS serializes emptied populated multi-reference lists as null; structural checks treat null as empty without changing existing untouched empty arrays.

The removed 28 references addressed 25 unique HCHR-owned blocks. Even `Shared` audience blocks were tied to HCHR; the strict validator requires matching Institute ownership. Original 43 blocks, their draft states, all HCHR fields/reference order and the four EHS-owned Delegate date blocks remain exact. EHS Sponsor's key-date list remains empty. No copied timetable, accommodation or extension claims were installed. EHS reservation fields and Institute hotel/transport/agenda-link empties remain native pending states.

Rollback only these documented EHS field values/reference arrays through CMS, queue the two items for site publication, and publish to staging only. Preserve original IDs and unrelated work. Restore neither the whole site nor shared blocks. Prepared missing-page artifacts require no CMS rollback because no import occurred.

## Existing content requiring Operations replacement or approval

For all four originals, approve or replace the exact retained field values in the matrix. Specific inherited claims needing attention:

| Register                 | Exact interim value/scope                                                                                                             | Required replacement                                                                                                                                 |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| 36-existing-ehs-copy     | Nine Delegate/seven Sponsor field changes in `existing-delta.json`; seven labeled copy placements across the two pages                | Final welcome/preparation/experience/FAQ/agenda copy and correctly scoped ordered blocks                                                             |
| 36-existing-hub-timing   | All four `hub-body` fields say credentials are typically supplied 8–10 weeks before the event                                         | Operations confirms audience-specific platform workflow, timing and final copy; do not propagate timing into new pages                               |
| 36-existing-reservations | Both HCHR page-level reservation fields include Attendee Hub Book Hotel, two nights, $335 plus $25 nightly resort fee                 | Independently confirm Delegate/Sponsor eligibility, rates, deadlines and booking workflow; identical stored paragraphs are not universal policy      |
| 36-existing-links        | Both Sponsor `hub-url` and `support-url`: `https://example.com/sponsor-hub`; Delegate mailto values above                             | Task 27 pending noninteractive/accurate contact treatment, then exact approved independent destinations                                              |
| 36-existing-staff        | HCHR retains Katrina ID `6392772fccd80e4fbe026044`; EHS retains Tori ID `6866abc99995b61355251a42`                                    | Confirm assignments for those events only; new 50 pages remain unassigned                                                                            |
| 36-existing-dates        | Four EHS Delegate native dates retained, but rendered badges DEC 19/DEC 19/JAN 14/JAN 15 differ from body Dec 18/Dec 18/Jan 13/Jan 14 | Operations supplies exact intended deadlines/timezones; task 37 verifies label/calendar consistency without guessing which representation is correct |
| 36-coverage              | 54 provisional planning rows, no approved audience exceptions                                                                         | Confirm event list and both audience requirements, corrections/cancellations/completeness, approver/date/source                                      |

## Staging verification

Both changed EHS items were saved, then **Queue for next site publish** restored their original non-draft status. CMS Publish now was never used. [Destination snapshot](publish-destinations.txt) and [screenshot](publish-destinations.png) show only `ipmi.webflow.io` checked, production unchecked. Site publication completed around 10:26 UTC; staging showed Published a few seconds ago while production remained seven hours old. All four final `isDraft`/`isArchived` values are false; the original draft block remains draft.

Chrome verified the two changed URLs at actual 1912px and 390px widths: document scrollWidth equals clientWidth; native Delegate/Sponsor audience, `noindex,follow`, stable anchors, correct EHS event/venue, Tori card and scoped colors remain. Preparation/Agenda/Experience/FAQ emptied sections display existing pending notices; Sponsor Experience displays one notice, not duplicates. EHS Delegate retains four date controls; Sponsor has its empty key-date notice. Hotel/reservation/transport remain native sparse fallbacks. No contact/Hub external destination was activated and no submission occurred.

Saved [Delegate desktop](ehs-delegate-desktop.png), [Delegate mobile](ehs-delegate-mobile.png), [Sponsor desktop](ehs-sponsor-desktop.png), [Sponsor mobile](ehs-sponsor-mobile.png) images were inspected as files. Initial full-page capture retries produced scaled strips; these were replaced with settled ordinary screenshots and actual saved dimensions checked: Delegate mobile 390×844, Sponsor desktop 1912×970. DOM `innerWidth/clientWidth` checks independently verify the viewport. Native Sponsor Experience's tall section leaves excess whitespace with sparse content, and its empty notice inherits white text on light gray; task 37 must improve sparse sizing and notice contrast. No code change was needed for content safety.

[Root CMS verification](task36-root-cms-verification.json) independently confirms all 43 block fieldData unchanged, zero cross-event references, preserved dates/staff/IDs and only expected deltas. [Root HTTP verification](task36-root-http-verification.json) confirms original HCHR text/image/link arrays exact, wrong EHS discipline absent, unchanged runtime/CSS pins, noindex and anchors preserved. Delegate link delta is five removed FAQ-toggle anchors. [Root matrix verification](task36-root-matrix-verification.json) checks the 54 pairs and 50 CSV candidates against native facts.

`vp node docs/web-refresh/evidence/task36/validate-handoff.mjs` passes the structural handoff checks in [results](validation-results.json). The original strict final-content validator/fixture is unchanged; it does not establish 54 live pages or approve missing leads/links/dates. Scoped `vp fmt --check`, `git diff --check` and local Markdown link checks passed before commit. Full repository verify still has pre-existing formatting failures recorded by task 34 and assigned to task 37; no runtime build claim applies because no runtime changed.

Protected `ipmi-custom-styles.css` SHA256 remains `80EF3C254BEF30F97398B63B0888A934085BB9B371CE4EE74E2E6B20BEFE4496`. Browser viewport override was reset; owned QA tab closed; existing Designer/user tabs retained. Chrome is released for the next worker. No production publish or external provider write occurred.

Task 37 date handoff: the four preserved EHS Delegate blocks have authored eyebrow/body dates Dec 18, Dec 18, Jan 13 and Jan 14; their `calendar-start`/`calendar-end` values are Dec 19T01:00Z (twice), Jan 14T01:00Z and Jan 15T01:00Z, with `all-day: true`. These instants fall on the previous evening in America/Toronto. The runtime's `calendarDataFromElement` converts native local source to UTC then extracts the UTC date for all-day events; `setupCalendars` rewrites the badges accordingly. This likely explains the observed one-day badge difference. Preserve authored deadlines and timed-instant behavior; reconcile local all-day semantics, visible badge/body and ICS using meaningful timezone-boundary tests in task 37. Task 36 did not change these values or runtime code.

Native RTE edit lesson for later tasks: a semantic fill can alter visible rich text without committing the complete Webflow editor document. Use separate native selection, deletion and typing calls with focus retained; inspect readback before moving focus, then reopen the item and independently read stored CMS data. A visible preview/saved toast alone is insufficient. This task detected and removed a stale second paragraph before queueing or staging. No CSV correction import was performed.
